# Affiliate Link Batch

**Session start (mandatory):** Execute `.grok/skills/affiliate-link-batch/SKILL.md` **SESSION-START** mode before any other work.

**Session end:** Handled by SessionEnd hook → same skill **SESSION-END** mode. Manual: `/affiliate-link-batch save`.

This skill is **only** for loading/saving `.grok/PROJECT_CONTEXT.md` — not for general extension tasks.